# eslint-config-xo-avanti

## Install

```
$ npm install --save-dev xo eslint-config-xo-avanti
```

## Usage

Add some configs to your `package.json`:

```json
{
  "name": "my-awesome-project",
  "scripts": {
    "lint": "xo"
  },
  "xo": {
    "extends": "xo-avanti"
  }
}
```
